# 통계적 분석 기능 가이드 v1.0

## 📌 개요

본 문서는 APC 제어 효과를 통계적으로 검증하기 위한 분석 기능에 대한 종합 가이드입니다.

### 주요 기능
1. **비제어 구간(대조군) 추출**: 제어가 없었던 구간을 자동으로 샘플링
2. **통계적 유의성 검정**: p-value, Effect Size, Cpk 등 다양한 지표 계산
3. **제어 vs 비제어 비교**: 순수 제어 효과 측정
4. **시각화**: 통계 분석 결과를 직관적으로 표현

---

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    APC 원본 데이터                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│           apc_preprocessor v1.3                              │
│  - 1st: 변경점 감지                                          │
│  - 2nd: 그룹화                                                │
│  - 3rd: 유의미한 구간 필터링                                  │
│  - 4th: 제어 구간 정보 저장          ← NEW                   │
│  - 5th: 비제어 구간 샘플링           ← NEW                   │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│       densitometer_preprocessor v1.3                         │
│  - 제어 구간 밀도계 데이터 추출                               │
│  - 비제어 구간 밀도계 데이터 추출    ← NEW                   │
│  - control_type 칼럼 추가            ← NEW                   │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│           zone_analyzer v1.3                                 │
│  - Zone별 분포 분석                                           │
│  - 통계 분석 수행                    ← NEW                   │
│    └─ StatisticalAnalyzer v1.0      ← NEW                   │
│       - p-value 검정                                         │
│       - Effect Size 계산                                     │
│       - Cpk 분석                                             │
└────────────┬────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────┐
│       statistical_visualizer v1.0           ← NEW            │
│  - p-value 히트맵                                            │
│  - Effect Size 비교                                          │
│  - Cpk 개선도                                                │
│  - 제어 vs 비제어 비교                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 통계 지표 설명

### 1. p-value (통계적 유의성)

**의미**: 관찰된 차이가 우연히 발생했을 확률

- **p < 0.05**: 통계적으로 유의미한 차이 (95% 신뢰도) ⭐
- **p < 0.01**: 매우 유의미한 차이 (99% 신뢰도)
- **p < 0.001**: 극히 유의미한 차이 (99.9% 신뢰도)

**검정 방법**:
- **Mann-Whitney U test** (비모수 검정) - **추천**
  - 분포 가정이 필요 없어 가장 안전
  - 중앙값 차이 검정
- **t-test** (모수 검정)
  - 정규분포 가정 필요
  - 평균값 차이 검정
- **KS-test** (Kolmogorov-Smirnov)
  - 전체 분포 차이 검정

### 2. Effect Size (효과 크기)

**의미**: 실제로 얼마나 큰 변화가 있었는지 측정

**Cohen's d**:
- **d < 0.2**: 무시할 수 있는 효과 (negligible)
- **0.2 ≤ d < 0.5**: 작은 효과 (small)
- **0.5 ≤ d < 0.8**: 중간 효과 (medium) ⭐
- **d ≥ 0.8**: 큰 효과 (large) ⭐

**중요**: p-value가 유의해도 효과 크기가 작으면 실질적 의미가 없을 수 있음!

### 3. Cpk (공정 능력 지수)

**의미**: UCL/LCL 내에 데이터가 얼마나 잘 수렴하는지

**등급**:
- **Cpk < 1.0**: 불량 (poor)
- **1.0 ≤ Cpk < 1.33**: 보통 (fair)
- **1.33 ≤ Cpk < 1.67**: 양호 (good) ⭐
- **Cpk ≥ 1.67**: 우수 (excellent) ⭐

**해석**:
- **Cpk 증가** = 제어 후 데이터가 더 안정적으로 목표 범위 내에 수렴

### 4. 분산 변화

**의미**: 데이터 변동성 개선 여부

- **variance_ratio < 1**: 분산 감소 (안정화) ⭐
- **variance_ratio > 1**: 분산 증가 (불안정)

---

## 🔧 사용 방법

### Step 1: Config 파일 업데이트

`preprocess_config_v1.3.txt` 또는 새로운 config 파일에서 다음 설정 확인:

```python
class PreprocessConfig:
    # 출력 파일명
    OUTPUT_4TH_CONTROL = '4th_control_regions.xlsx'
    OUTPUT_5TH_NO_CONTROL = '5th_no_control_regions.xlsx'
    OUTPUT_STATISTICAL_ANALYSIS = 'statistical_analysis_results.xlsx'

    # 비제어 구간 샘플링 설정 (필요시 추가)
    NO_CONTROL_MIN_GAP_MINUTES = 10  # 제어점으로부터 최소 간격
```

### Step 2: APC 전처리 실행

```python
from apc_preprocessor_v1_3 import APCPreprocessor
from preprocess_config_v1_3 import PreprocessConfig
import logging

# Config 및 Logger 설정
config = PreprocessConfig()
logger = logging.getLogger('preprocessing')

# APC Preprocessor 실행
apc_processor = APCPreprocessor(config, logger)
meaningful_df = apc_processor.run(
    input_file='path/to/apc_data.xlsx',
    llspec_file='path/to/llspec.xlsx'  # 옵션
)

# 출력 파일:
# - 4th_control_regions.xlsx: 제어 구간 정보
# - 5th_no_control_regions.xlsx: 비제어 구간 정보 (대조군)
```

### Step 3: 밀도계 데이터 추출

```python
from densitometer_preprocessor_v1_3 import DensitometerPreprocessor

# Densitometer Preprocessor 실행
densitometer_processor = DensitometerPreprocessor(config, logger)
extracted_data = densitometer_processor.run(
    control_regions_file='outputs/4th_control_regions.xlsx',
    no_control_regions_file='outputs/5th_no_control_regions.xlsx',
    raw_data_file='path/to/densitometer_raw.xlsx'
)

# 출력 파일:
# - extracted_densitometer_data.xlsx: 제어 + 비제어 구간 데이터
#   (control_type 칼럼 포함: 'controlled' vs 'no_control')
```

### Step 4: 통계 분석 실행

```python
from zone_analyzer_v1_3 import ZoneAnalyzer
from statistical_analyzer_v1_0 import StatisticalAnalyzer

# Statistical Analyzer 생성
stat_analyzer = StatisticalAnalyzer(logger)

# Zone Analyzer 실행 (통계 분석 포함)
zone_analyzer = ZoneAnalyzer(config, stat_analyzer, logger)

zone_results, stat_results = zone_analyzer.run(
    densitometer_data=extracted_data,
    meaningful_changes=meaningful_df,
    visualize=True,
    perform_statistical_analysis=True
)

# 출력 파일:
# - zone_analysis_results.xlsx: Zone별 분석 결과
# - statistical_analysis_results.xlsx: 통계 분석 결과
```

### Step 5: 시각화 (옵션)

```python
from statistical_visualizer_v1_0 import StatisticalVisualizer

# Visualizer 생성
visualizer = StatisticalVisualizer(config, logger)

# 시각화 생성
visualizer.create_statistical_summary_plots(stat_results)

# 출력 파일:
# - outputs/plots/statistical_analysis/pvalue_heatmap.png
# - outputs/plots/statistical_analysis/effect_size_comparison.png
# - outputs/plots/statistical_analysis/cpk_improvement.png
# - outputs/plots/statistical_analysis/control_effectiveness_summary.png
# - outputs/plots/statistical_analysis/control_vs_no_control.png
```

---

## 📈 결과 해석 가이드

### 통계 분석 결과 파일 (`statistical_analysis_results.xlsx`)

주요 칼럼:

| 칼럼명 | 설명 | 해석 |
|--------|------|------|
| `group_id` | 그룹 ID | 제어 구간 식별자 |
| `zone_id` | Zone ID | Zone 구역 번호 |
| `control_type` | 제어 유형 | 'controlled' or 'no_control' |
| `mannwhitney_pvalue` | Mann-Whitney p-value | < 0.05이면 통계적 유의 |
| `cohens_d` | Cohen's d | 효과 크기 (절댓값 > 0.5면 의미있음) |
| `effect_size_category` | 효과 크기 범주 | 'small', 'medium', 'large' |
| `before_cpk` | 제어 전 Cpk | - |
| `after_cpk` | 제어 후 Cpk | - |
| `cpk_improved` | Cpk 개선 여부 | True면 개선됨 |
| `variance_reduced` | 분산 감소 여부 | True면 안정화됨 |
| `statistically_significant` | 통계적 유의성 | p < 0.05 |
| `practically_significant` | 실질적 유의성 | Effect size ≥ medium |
| `control_effective` | 제어 효과 있음 | 종합 판단 |

### 해석 예시

#### ✅ 제어 효과가 있는 경우

```
group_id: 1, zone_id: 5, control_type: 'controlled'
- mannwhitney_pvalue: 0.0023  ← p < 0.05 (통계적 유의)
- cohens_d: 0.68              ← medium effect
- cpk_improved: True          ← Cpk 개선
- variance_reduced: True      ← 분산 감소
- control_effective: True     ← 제어 효과 있음 ✓
```

**결론**: Zone 5에서 제어가 통계적으로 유의미하고, 실질적인 효과도 있음

#### ❌ 제어 효과가 없는 경우

```
group_id: 2, zone_id: 3, control_type: 'controlled'
- mannwhitney_pvalue: 0.342   ← p > 0.05 (통계적 유의 아님)
- cohens_d: 0.12              ← negligible effect
- cpk_improved: False         ← Cpk 개선 안 됨
- variance_reduced: False     ← 분산 감소 안 됨
- control_effective: False    ← 제어 효과 없음 ✗
```

**결론**: Zone 3에서 제어 효과가 미미함

#### 🔍 비제어 구간 (대조군)

```
group_id: 1, zone_id: 5, control_type: 'no_control'
- mannwhitney_pvalue: 0.652   ← p > 0.05 (변화 없음)
- cohens_d: 0.08              ← negligible effect
- control_effective: False    ← 자연 변화 없음
```

**결론**: 제어 구간에서는 유의미한 변화가 있었지만, 비제어 구간에서는 변화가 없음
→ **순수 제어 효과 확인** ✓

---

## 🎨 시각화 결과 해석

### 1. p-value 히트맵 (`pvalue_heatmap.png`)

- **녹색**: p-value가 낮음 (통계적 유의) ⭐
- **빨간색**: p-value가 높음 (유의하지 않음)

### 2. Effect Size 비교 (`effect_size_comparison.png`)

- **회색 막대**: negligible (무시 가능)
- **파란색 막대**: small (작은 효과)
- **주황색 막대**: medium (중간 효과) ⭐
- **빨간색 막대**: large (큰 효과) ⭐

### 3. Cpk 개선도 (`cpk_improvement.png`)

- **빨간색 막대 (Before)**: 제어 전 Cpk
- **녹색 막대 (After)**: 제어 후 Cpk
- **녹색이 빨간색보다 높으면**: Cpk 개선 ⭐

### 4. 제어 효과 요약 (`control_effectiveness_summary.png`)

- **막대 그래프**: 각 기준별 개수
- **파이 차트**: 전체 중 제어 효과 있는 비율

### 5. 제어 vs 비제어 비교 (`control_vs_no_control.png`)

- **파란색 막대**: 제어 구간의 변화량
- **회색 막대**: 비제어 구간의 변화량 (대조군)
- **파란색이 회색보다 크면**: 순수 제어 효과 있음 ⭐

---

## ⚠️ 주의사항

### 1. 통계적 유의성 vs 실질적 유의성

- **p-value만으로 판단하지 말 것!**
- Effect Size도 함께 고려해야 함
- 예: p < 0.05이지만 Cohen's d = 0.1이면 실질적 의미 없음

### 2. 비제어 구간 샘플 수

- 비제어 구간 샘플 수가 제어 구간과 유사해야 공정한 비교
- 기본값: 제어 구간 수와 동일하게 샘플링

### 3. UCL/LCL 값

- 비제어 구간은 제어 구간의 평균 UCL/LCL 사용
- 더 정확한 분석을 위해서는 시점별 UCL/LCL 매칭 고려

### 4. 데이터 품질

- 중복 데이터 제거 확인
- 이상치(outlier) 검토
- 충분한 데이터 포인트 (각 구간 최소 30개 이상 권장)

---

## 📚 참고 문헌

### 통계 검정
- Mann-Whitney U test: 비모수 검정의 표준
- Cohen's d: Effect size 측정의 표준

### 공정 능력
- Cpk: AIAG (Automotive Industry Action Group) 표준
- Process Capability Index: ISO 표준

---

## 🔄 버전 히스토리

### v1.0 (2024-02-11)
- 초기 버전
- 비제어 구간 추출 기능
- 통계 분석 기능 (p-value, Effect Size, Cpk)
- 시각화 기능

---

## 💬 FAQ

### Q1: p-value가 0.05보다 크면 제어 효과가 없다는 뜻인가요?

**A**: 아닙니다. p > 0.05는 "통계적으로 유의미한 차이를 발견하지 못했다"는 의미입니다.
실제로 효과가 있을 수 있지만, 데이터가 부족하거나 변동성이 커서 감지하지 못한 것일 수 있습니다.
Effect Size와 Cpk도 함께 확인하세요.

### Q2: Cohen's d가 음수인 경우는 어떻게 해석하나요?

**A**: 음수는 "감소" 방향의 변화를 의미합니다. 절댓값으로 효과 크기를 판단하면 됩니다.
예: Cohen's d = -0.6 → medium effect (감소 방향)

### Q3: 제어 구간과 비제어 구간 모두에서 유의미한 변화가 있으면?

**A**: 이는 시간 경과에 따른 자연적 변화일 가능성이 있습니다.
이 경우 제어 효과를 단정하기 어려우므, "net control effect" (제어 구간 변화 - 비제어 구간 변화)를 확인하세요.

### Q4: 비제어 구간을 찾지 못하면?

**A**: 제어가 너무 빈번하게 발생하여 비제어 구간을 샘플링하기 어려운 경우입니다.
`min_gap_minutes` 값을 줄이거나, 더 긴 시간 범위의 데이터를 사용하세요.

### Q5: Zone별로 결과가 다른데, 어떻게 해석하나요?

**A**: 이는 정상적인 현상입니다. 각 Zone마다 제어 민감도가 다를 수 있습니다.
- **Zone 1-3**: 입구 쪽 (민감도 높음)
- **Zone 5-7**: 중앙 (가장 안정적)
- **Zone 9-11**: 출구 쪽 (민감도 높음)

---

## 📞 문의

추가 질문이나 개선 제안은 프로젝트 관리자에게 문의하세요.

**Happy Statistical Analysis! 📊✨**
